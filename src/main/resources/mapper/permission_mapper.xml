<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.kocesat.mybatisdemo.mapper.channel.PermissionMapper">
    <resultMap type='Permission' id='PermissionResultMap'>
        <id property='code' column='code'/>
        <result property='nameTr' column='name_tr'/>
        <result property='nameEn' column='name_en'/>
        <result property='type' column='type'/>
        <result property='status' column='status'/>
        <result property='createdUser' column='created_user'/>
        <result property="createdAt" column="created_at" javaType="java.time.LocalDateTime" />
        <result property="modifiedAt" column="modified_at" javaType="java.time.LocalDateTime" />
    </resultMap>

    <insert id="insert" parameterType="Permission">
        insert into tpermission (
            code, name_tr, name_en, type, status
        ) values (#{code}, #{nameTr}, #{nameEn}, #{type}, #{status})
    </insert>

    <select id="select" resultType="Permission">
        select * from tpermission
    </select>

</mapper>


